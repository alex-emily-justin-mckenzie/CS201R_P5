#pragma once

using namespace std;

class Bank {
public:
	// payPlayers
	// depositMoney
	// updateRentAmount on properties when someone owns it or buys houses
	// map of which properties are owned and by whom (key is property)
	// mortgage?

	// should the bank handle the chance/community chest cards? I'm thinking categorize the cards,
	// then it randomly chooses a type (pay, get paid, or move) then based on type then it picks a card of that type
	// then each one can use the same function more or less with different inputs (amount of money or how many spaces to move to)
};